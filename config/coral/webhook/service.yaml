apiVersion: v1
kind: Service
metadata:
  labels:
    app: coral
  name: coral-webhook-service
  namespace: coral-system
spec:
  type: ClusterIP
  ports:
    - port: 443
      protocol: TCP
      targetPort: 9443
  selector:
    app: coral
